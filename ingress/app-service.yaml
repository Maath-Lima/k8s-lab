apiVersion: v1
kind: Service
metadata:
  namespace: giropops
  name: giropops-senhas
  labels:
    app: giropops-senhas
spec:
  selector:
    app: giropops-senhas
  ports:
    - name: tcp-app
      protocol: TCP
      port: 5000
      targetPort: 5000
